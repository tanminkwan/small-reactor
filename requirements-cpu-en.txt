# CPU-only dependencies for Small Reactor
# GPU-related packages excluded for CPU optimization

# Core dependencies
gradio>=4.0.0
insightface>=0.7.3
opencv-python>=4.8.0
numpy>=1.24.0
python-dotenv>=1.0.0

# CodeFormer dependencies
codeformer-pip>=0.0.4

# AI/ML dependencies (CPU optimized)
#diffusers[torch]>=0.35.1
#transformers>=4.56.1
#accelerate>=1.10.1
#safetensors>=0.6.2

# ONNX Runtime (CPU only)
onnxruntime>=1.15.0

# PyTorch (CPU only)
torch>=2.0.0+cpu --index-url https://download.pytorch.org/whl/cpu
torchvision>=0.15.0+cpu --index-url https://download.pytorch.org/whl/cpu

# CPU optimization packages
# Intel MKL-DNN optimization (optional)
# intel-extension-for-pytorch>=2.0.0  # Intel CPU optimization

# Testing dependencies
pytest>=7.0.0
pytest-cov>=4.0.0
pytest-mock>=3.10.0
pytest-asyncio>=0.21.0
pytest-xdist>=3.0.0
pytest-html>=3.1.0
pytest-benchmark>=4.0.0

# Development dependencies
black>=23.0.0
flake8>=6.0.0
mypy>=1.0.0
pre-commit>=3.0.0

# CPU-only installation guide:
# pip install -r requirements-cpu-en.txt
# 
# For Intel CPU optimization:
# pip install intel-extension-for-pytorch
